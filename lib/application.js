$(document).ready(function(){
	
	// Slide Funktion auf der Home Seite
	$("#slides").slides({
		container: 'slides_container',
		pagination: false,
		generatePagination: false,
		play: 8000,
		fadeSpeed: 1000,
		slideSpeed: 1200
	})
	
})